CREATE DATABASE pgmv CHARSET latin1 COLLATE latin1_general_cs;
USE pgmv;
CREATE TABLE usuario(
	nome VARCHAR(100) PRIMARY KEY,
    login VARCHAR(100) NOT NULL,
    senha VARCHAR(100) NOT NULL
);
INSERT INTO usuario VALUES("Nometeste","teste","teste");
select * from usuario;